// Vue adapter entry: the reactive `useNetwork()` composable. `vue` is an
// optional peer dependency and is externalized at build.
export { useNetwork } from './adapters/vue/use-network'
export type { BISNetwork, BISSession, BISWallet, BISWalletProvider } from './types/common'
